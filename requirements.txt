# =============================================================================
# MediMap AI — Production Requirements
# Target: Windows 11 | Python 3.10+ | CUDA-enabled GPU
# =============================================================================

# ── Core ML / DL ─────────────────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0
# Install CUDA wheels separately:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

scikit-learn>=1.3.2
numpy>=1.26.0
pandas>=2.1.3
scipy>=1.11.4
joblib>=1.3.2

# ── Computer Vision ───────────────────────────────────────────────────────────
Pillow>=10.1.0
opencv-python>=4.8.1.78
albumentations>=1.3.1          # Image augmentation pipeline

# ── GIS / Geospatial ─────────────────────────────────────────────────────────
folium>=0.15.0
geopy>=2.4.1
requests>=2.31.0
streamlit-folium>=0.18.0
openlocationcode>=1.0.1      # Decode Google Plus Codes (e.g. 5M23+GR3) offline

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit>=1.30.0
streamlit-option-menu>=0.3.12
streamlit-geolocation>=0.0.10     # Browser GPS — pinpoints user location
plotly>=5.18.0
watchdog>=3.0.0                # Hot-reload for Streamlit dev

# ── Data Handling ─────────────────────────────────────────────────────────────
pyarrow>=14.0.1
openpyxl>=3.1.2
imbalanced-learn>=0.11.0       # SMOTE for class imbalance

# ── Model Persistence & Experiment Tracking ───────────────────────────────────
mlflow>=2.9.2
onnx>=1.15.0
onnxruntime-gpu>=1.16.3        # ONNX inference with CUDA

# ── Configuration & Logging ───────────────────────────────────────────────────
pydantic>=2.5.2
python-dotenv>=1.0.0
loguru>=0.7.2
rich>=13.7.0                   # Beautiful terminal output

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4.3
pytest-cov>=4.1.0

# ── Dev / Lint ────────────────────────────────────────────────────────────────
black>=23.11.0
ruff>=0.1.7
mypy>=1.7.1
google-genai>=0.2.0
